/** Re-export for `@/lib/utils/getGreeting` imports; implementation lives in `@zyra/shared`. */
export {
  firstNameFromDisplayName,
  formatGreetingWithName,
  getGreetingPhrase,
  GREETING_NEUTRAL_FALLBACK,
} from "@shared/utils/getGreeting";
